---
platform: linux

inputs:
- name: ci
- name: cf-deployment-concourse-tasks
- name: toolsmiths-env

outputs:
- name: created-acceptance-test-configs

params:
  DEBUG: false
  CONFIGS:
  ENVS:

run:
  path: ci/shared/tasks/create-acceptance-test-configs/task.bash
